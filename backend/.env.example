# Optional: enable OpenAI-backed summarization and generic Q&A fallback.
OPENAI_API_KEY=
# Set this to any Responses API model you have access to.
OPENAI_MODEL=gpt-5.2

# Retrieval mode: tfidf (default) or embeddings
RETRIEVER_MODE=tfidf
SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2
